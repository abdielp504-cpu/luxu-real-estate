import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Acerca de | LUJO",
  description: "Conozca la historia detrás de la inmobiliaria más exclusiva.",
}

export default function AboutPage() {
  return (
    <main className="flex min-h-screen flex-col items-center">
      {/* Hero Section de la página */}
      <section className="relative flex h-[40vh] w-full items-center justify-center bg-zinc-950 text-white">
        <div className="container relative z-10 text-center">
          <h1 className="font-serif text-5xl tracking-tighter sm:text-7xl">NUESTRA HISTORIA</h1>
          <p className="mx-auto mt-4 max-w-[600px] text-zinc-400 font-sans tracking-widest uppercase text-xs">
            Redefiniendo el estándar de la vida excepcional desde 2026.
          </p>
        </div>
      </section>

      {/* Contenido Principal */}
      <section className="container py-24">
        <div className="grid gap-12 lg:grid-cols-2 lg:items-center">
          <div className="space-y-6">
            <h2 className="font-serif text-3xl leading-tight">
              Más que una propiedad, <br /> 
              <span className="text-zinc-500 italic">un legado de distinción.</span>
            </h2>
            <p className="text-muted-foreground leading-relaxed">
              En LUJO, entendemos que una residencia de alto nivel no se define solo por sus metros cuadrados, 
              sino por la experiencia y el estatus que otorga a sus habitantes. 
              Nos especializamos en conectar a visionarios con los santuarios más exclusivos del mundo.
            </p>
            <div className="grid grid-cols-2 gap-8 pt-6">
              <div>
                <p className="text-3xl font-serif">30+</p>
                <p className="text-xs uppercase tracking-widest text-muted-foreground">Propiedades Elite</p>
              </div>
              <div>
                <p className="text-3xl font-serif">100%</p>
                <p className="text-xs uppercase tracking-widest text-muted-foreground">Privacidad Garantizada</p>
              </div>
            </div>
          </div>
          
          <div className="aspect-square bg-zinc-100 dark:bg-zinc-900 rounded-sm overflow-hidden relative border">
             {/* Aquí podrías poner una imagen tuya o de una oficina de lujo */}
             <div className="absolute inset-0 flex items-center justify-center text-zinc-300 font-serif italic">
               Imagen de la Mansión Principal
             </div>
          </div>
        </div>
      </section>
    </main>
  )
}